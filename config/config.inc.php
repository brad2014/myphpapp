<?php

#
# See default_config.php for what can be set, and what the defaults are.
#
$default_config =  Array();

# connection to database server
$default_config['db_password'] = 'changeme!';   # Don't forget to change!

# default widget colors
$default_config['colors'] = Array( 'red', 'green' );
